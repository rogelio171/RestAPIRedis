services:
  redis:
    image: 'redis:8'
    ports:
      - '6379'
  postgres:
    image: 'postgres:17'
    environment:
      POSTGRES_DB: countries_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - '5432'
